## Goal

<goal>

## How the loop ended

<outcome>

| | |
| ------------------ | ------------------------------------- |
| Outcome            | `<status>`                            |
| Rounds             | <iter> of <max>                       |
| Last stop check    | `<last>`                              |

The stop check every round was measured against:

```
<until>
```

## Reviewing this

Every commit here was written by an agent working unattended, one round at a
time, against the goal above. No round was reviewed as it landed and no human
saw the intermediate states. The stop check is the only evidence the work is
finished — read it as a claim to verify, not as a guarantee, and check that it
actually proves the goal rather than merely passing.
